name: microservice/02-ticking-app/tests-auth

on:
  pull_request:
    branches:
      - microservices-02-ticketing-app
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        run: cd microservice/02-ticking-app/auth && npm install && npm run test:ci
